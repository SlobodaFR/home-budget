/**
 * SOURCE: compte d'entree d'argent (salaire, revenus). Le foyer en possede un seul.
 * EXPENSE: compte de prelevement (depenses courantes). Le foyer en possede trois.
 */
export enum AccountType {
  SOURCE = 'SOURCE',
  EXPENSE = 'EXPENSE',
}
